{
  "name": "pipelog",
  "author": "aokihu",
  "version": "1.0.0",
  "description": "Using named pipes for output debugging",
  "keywords": [
    "log",
    "debug",
    "develop"
  ],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "maintainers": [
    "aokihu"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "bunx tsc && bun build src/index.ts --target node --format esm --minify --outdir dist",
    "test": "bun test ./src/test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^25.0.9"
  }
}
